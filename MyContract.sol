pragma solidity ^0.6.0;

contract MyContract {
    // state variable
    // local variable
    
    uint value = 1;
    
    function getValue() public pure returns(uint) {
        uint value = 1;
        return value;
    }
    
}